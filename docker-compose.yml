#version: '3.8'
#
#services:
#  portal:
#    build: .
#    container_name: portal-application
#    ports:
#      - "8082:8082"
#    environment:
#      - SPRING_PROFILES_ACTIVE=docker
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://host.docker.internal:5432/ink
#      - SPRING_DATASOURCE_USERNAME=postgres
#      - SPRING_DATASOURCE_PASSWORD=postgres
#      - FILE_STORAGE_PATH=/app/file-storage
#      - ONLYOFFICE_URL=http://onlyoffice:80
#      - ONLYOFFICE_JWT_SECRET=supersecret
#    volumes:
#      - ./file-storage:/app/file-storage
#      - ./logs:/app/logs
#    restart: always
#    networks:
#      - portal_net
#
#  onlyoffice:
#    image: onlyoffice/documentserver:5.5.1
#    container_name: onlyoffice
#    restart: always
#    ports:
#      - "8083:80"
#    environment:
#      # ПРОСТЫЕ настройки
#      - JWT_ENABLED=false
#      - JWT_SECRET=supersecret
#      - JWT_HEADER=Authorization
#
#      # Используем встроенные сервисы (localhost)
##      - DB_TYPE=builtin  # Встроенная БД
##      - MYSQL_SERVER_HOST=localhost
##      - AMQP_URI=amqp://guest:guest@localhost
#
#      # Отключаем лишние проверки
#      - CHECK_HEALTH=false
#
#      # Оптимизация памяти
#      - NODE_OPTIONS=--max-old-space-size=2048
#    volumes:
#      - onlyoffice_data:/var/www/onlyoffice/Data
#      - onlyoffice_logs:/var/log/onlyoffice
#    deploy:
#      resources:
#        limits:
#          memory: 3G
#          cpus: '2.0'
#    networks:
#      - portal_net
#
#volumes:
#  onlyoffice_data:
#  onlyoffice_logs:
#
#networks:
#  portal_net:
#    driver: bridge

version: '3.8'

services:
  portal:
    build: .
    container_name: portal-application
    ports:
      - "8082:8082"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:postgresql://host.docker.internal:5432/ink
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=postgres
      - FILE_STORAGE_PATH=/app/file-storage
      # URL для интеграции с OnlyOffice внутри сети
      - ONLYOFFICE_URL=http://onlyoffice:80
      - ONLYOFFICE_JWT_SECRET=supersecret
    volumes:
      - ./file-storage:/app/file-storage
      - ./logs:/app/logs
    restart: unless-stopped
    networks:
      - portal_net

  onlyoffice:
    image: onlyoffice/documentserver:5.5.1   # версия без лимита подключений
    container_name: onlyoffice
    restart: always
    ports:
      - "8083:80"   # доступ снаружи по http://localhost:8083
    environment:
      - JWT_ENABLED=true
      - JWT_SECRET=supersecret
      - JWT_HEADER=Authorization
    networks:
      - portal_net

networks:
  portal_net:
    driver: bridge
